
function Map(posts) {
  this._posts = posts;
}

Map.prototype.find_a_person = function(name) {
  return [];
  };

Map.prototype.find_if_person_exists = function(name) {
  return Boolean;
  };

module.exports = Map;
